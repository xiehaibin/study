1. size() ;  // 讲算字符串的长度

2.   +    ;  // 将两个字符串连接起来组成一个新的字符串

3. substr(int, int) ;// 返回字符串的子串 第一个参数是子串在字符串中的位置 第二个参数是要截取的长                        度

4. find(string, int); // 对象名称.find(要查找的字符串，开始查找的位置); 返回查找到字符串在主串的                         位 没找到返回-1。 如果不给出位置参数，默认位置参数为0;

5. str.swap(str2) ;  // 用来交换两个对象的属性

6. begin();  // 第一元素的指针

7. end();    // 最后一个元素之后的字符串标记